var page = require('webpage').create();

console.log(system);

phantom.onError = function(msg, trace) {
  console.log(msg);
  if (trace) {
    console.log(trace.join('\n'));
  }
  phantom.exit(1);
};

page.onConsoleMessage = function(msg) {
  console.log(msg);
};

page.open('Testling.html', function() {
  page.evaluate(function(){});
  phantom.exit();
});

